import { OpenIDAuthenticationRequest } from './open-idauthentication-request';

describe('OpenIDAuthenticationRequest', () => {
  it('should create an instance', () => {
    expect(new OpenIDAuthenticationRequest()).toBeTruthy();
  });
});
